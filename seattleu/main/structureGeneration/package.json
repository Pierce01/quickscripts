{
  "name": "structuregenmedia",
  "version": "1.0.0",
  "description": "",
  "main": "index.mjs",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild \"./\" --bundle --platform=node --outfile=dist/esb.js && cd ./dist && pkg ./esb.js --targets node18-win-x64 && cd .."
  },
  "author": "Pierce Harriz",
  "license": "ISC",
  "dependencies": {
    "prompt": "^1.3.0",
    "t4.ts": "^0.0.16"
  }
}
